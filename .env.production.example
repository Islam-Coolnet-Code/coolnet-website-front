# Production Environment Variables
# Copy to .env and update values before building

# CMS API URL - leave empty when using nginx proxy (recommended for Docker)
# Set to full URL if API is on a different domain
VITE_CMS_API_URL=

# Base URL for legacy wizard API
VITE_BASE_URL=https://portal.jet.net.il

# Docker compose port mapping
FRONTEND_PORT=80
